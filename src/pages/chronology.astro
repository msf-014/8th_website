---
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/header.astro";
import Layout from "../layouts/Layout.astro";
import Footer from "../components/footer.astro";
---

<BaseLayout>
  <head>
    <link rel="stylesheet" href="https://use.typekit.net/bwo1tdd.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link
      rel="stylesheet"
      href="https://fonts.adobe.com/fonts/acumin-variable?vf-instance=Semibold&vf-font-size=100&vf-font=AcuminVF"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Noto+Sans+JP:wght@700;900&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="bg_reveal" id="bg_reveal"></div>
    <div class="contents-list" transition:name="title--chronology">
      <div class="contents_texto">
        <span class="content_label">年表</span>
        <h2>CHRONOLOGY</h2>
        <a href="/" class="back_btn" aria-label="戻る">
          <span class="back_arrow">←</span>
        </a>
      </div>
      <div class="chronology">
        <div class="chronology_title">
          <h4>矢車りね活動記録</h4>
          <p>2018/09/28-2026/09/28</p>
        </div>
        <div class="timeline-wrap">
          <div id="timeline" class="timeline"></div>
        </div>
      </div>

      <Footer />

      <style>
        * {
          margin: 0;
          padding: 0;
          box-sizing: border-box;
        }

        html {
          scrollbar-color: #ffffff #dcc8ff;
          scroll-behavior: smooth;
        }

        body {
          background-color: #fff;
          position: relative;
        }

        :root {
          --morph-duration: 0.6s;
          --bg-reveal-duration: 0.4s;

          --timeline-line-color: #2f3b52;
          --timeline-circle-bg: #fff;
          --timeline-circle-border: #2f3b52;
          --timeline-year-color: #2f3b52;
          --timeline-text-sub: #000;
          --timeline-text-comments: #9f83cf;
          --timeline-max-width: 1900px;
          --header-back-color: rgb(220, 200, 255);

          /* 左テキスト列の幅。残りが右メディア列になる */
          --text-col-width: 38%;
        }

        ::view-transition-old(title--message),
        ::view-transition-new(title--message) {
          animation-duration: var(--morph-duration);
        }

        @font-face {
          font-family: "殴り書きクレヨン";
          src: url(public/crayon_1-1.ttf); fotmat("ttf");
        }

        .bg_reveal {
          position: fixed;
          inset: 0;
          /* 紫背景の上にドットを重ねる */
          background-color: #dcc8ff;
          background-image: radial-gradient(
            circle,
            rgba(120, 80, 200, 0.35) 1px,
            transparent 1px
          );
          background-size: 5px 5px;
          z-index: -1;
          transform: scaleY(0);
          animation: revealBg var(--bg-reveal-duration) ease forwards;
        }

        @keyframes revealBg {
          from {
            transform: scaleY(0);
          }
          to {
            transform: scaleY(1);
          }
        }

        .contents-list {
          display: block;
          box-shadow:
            0 -1px 0 0 #000,
            0 1px 0 0 #000;
        }

        .contents_texto {
          box-shadow:
            0 -1px 0 0 #000,
            0 1px 0 0 #000;
          padding: 14px 16px 14px;
          white-space: nowrap;
          position: relative;
          display: flex;
          align-items: center;
          gap: 24px;
          background: rgb(220, 200, 255);
          z-index: 1;
        }

        .content_label {
          position: absolute;
          top: 14px;
          right: 16px;
          font-size: 15px;
          color: #000;
          font-family: "Zen Kaku Gothic New", sans-serif;
          font-weight: 800;
          padding: 6px 14px;
        }

        .contents_texto h2 {
          font-size: 110px;
          font-family: "acumin-variable", sans-serif;
          font-variation-settings:
            "wght" 400,
            "wdth" 100,
            "slnt" 0;
          font-style: normal;
          align-items: center;
          line-height: 1;
          letter-spacing: -0.06em;
          position: relative;
        }

        .back_btn {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          width: 56px;
          height: 56px;
          border: 2px solid #000;
          border-radius: 50%;
          text-decoration: none;
          color: #000;
          flex-shrink: 0;
          transition:
            background 0.2s,
            color 0.2s;
        }
        .back_btn:hover {
          background: #000;
          color: #dcc8ff;
        }
        .back_arrow {
          font-size: 20px;
          line-height: 1;
        }

        .chronology_title {
          width: 30%;
          margin: auto;
          position: relative;
          padding: 0.5em 1.5em;
          border-bottom: 1px solid #000;
          margin-left: -1px; /* .chronologyのborder-left分だけはみ出す */
          margin-right: -1px; /* .chronologyのborder-right分だけはみ出す */
          width: calc(100% + 2px); /* はみ出した分だけ横幅を広げる */
        }
        .chronology_title h4 {
          display: block;
          font-family: "殴り書きクレヨン", sans-serif;
          font-weight: 500;
          text-align: center;
          font-size: 60px;
          margin: 0;
          padding: 45px 0 20px 0;
        }
        .chronology_title p {
          display: flex;
          font-family: "殴り書きクレヨン", sans-serif;
          font-weight: 500;
          align-items: center;
          justify-content: center;
          text-align: center;
          font-size: 30px;
          padding-bottom: 45px;
        }

        .chronology {
          /* timeline-wrapと端を揃えるため同じmax-widthで中央寄せ */
          max-width: var(--timeline-max-width);
          margin: 0 auto;
          position: relative;
          /* 左右にラインを引く */
          border-left: 1px solid #000;
          border-right: 1px solid #000;
          /* contents_textoの下線（box-shadow 1px）と隙間なく接続 */
          margin-top: -1px;
          background: rgb(220, 200, 255);
          opacity: 0;
          animation: timelineFadeIn 0.5s ease forwards;
          animation-delay: var(--bg-reveal-duration);
        }

        /* ===========================================================
           年表セクション（:global — JSで動的生成するため）
           =========================================================== */

        :global(.timeline-wrap) {
          max-width: var(--timeline-max-width);
          margin: 0 auto;
          margin-bottom: 100px;
          padding: 64px 50px;
          opacity: 0;
          animation: timelineFadeIn 0.5s ease forwards;
          animation-delay: var(--bg-reveal-duration);
        }

        @keyframes timelineFadeIn {
          from {
            opacity: 0;
            transform: translateY(8px);
          }
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }

        :global(.timeline) {
          position: relative;
        }

        /* 縦の通し線 */
        :global(.timeline-line) {
          position: absolute;
          left: 10px;
          width: 2px;
          background: var(--timeline-line-color);
          transform: translateX(-50%);
          z-index: 0;
        }

        /* 1項目のラッパー */
        :global(.timeline-item) {
          display: flex;
          align-items: flex-start;
          padding-bottom: 56px;
          position: relative;
          z-index: 1;
          /* スクロールフェードイン */
          opacity: 0;
          transform: translateY(16px);
          transition:
            opacity 0.5s ease,
            transform 0.5s ease;
        }
        :global(.timeline-item.is-visible) {
          opacity: 1;
          transform: translateY(0);
        }
        :global(.timeline-item:last-child) {
          padding-bottom: 0;
        }

        /* 丸マーカー列 */
        :global(.marker-col) {
          width: 20px;
          flex: 0 0 20px;
          display: flex;
          justify-content: center;
          padding-top: 4px;
        }
        :global(.marker-circle) {
          width: 16px;
          height: 16px;
          border-radius: 50%;
          background: var(--timeline-circle-bg);
          border: 3px solid var(--timeline-circle-border);
        }

        /* マーカーの右側 全体ラッパー → 2カラムに */
        :global(.content-col) {
          flex: 1 1 auto;
          padding-left: 32px;
          display: flex;
          align-items: flex-start;
          gap: 40px;
        }

        /* 左：テキスト列 */
        :global(.text-col) {
          flex: 0 0 var(--text-col-width);
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 10px;
        }

        /* 右：メディア列（YouTube） */
        :global(.media-col) {
          flex: 1 1 auto;
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 16px;
        }

        /* メディアがない項目はテキスト列を全幅に */
        :global(.content-col.no-media .text-col) {
          flex: 1 1 auto;
        }

        /* 年 */
        :global(.item-year) {
          display: inline-block;
          font-size: 13px;
          font-weight: 700;
          color: var(--timeline-year-color);
          letter-spacing: 0.05em;
        }

        /* タイトル */
        :global(.item-title) {
          margin: 0;
          font-size: 20px;
          font-weight: 700;
          padding-bottom: 10px;
          border-bottom: 2px solid var(--timeline-line-color);
        }

        /* 概要 */
        :global(.item-desc) {
          margin: 0;
          font-size: 14px;
          font-weight: 700;
          line-height: 2;
          color: var(--timeline-text-sub);
        }

        /* 一言コメント */
        :global(.item-comment) {
          position: relative;
          padding: 10px 14px;
          font-size: 13px;
          font-style: italic;
          color: var(--timeline-text-comments);
          background: #fbf9ff;
          border-radius: 999px;
          align-self: flex-start;
        }
        :global(.item-comment)::before {
          content: "";
          position: absolute;
          left: 30px;
          top: -15px;
          width: 0;
          height: 0;
          border-right: 15px solid transparent;
          border-bottom: 15px solid #fbf9ff;
          border-left: 15px solid transparent;
        }

        /* いいね */
        :global(.item-footer) {
          display: flex;
          align-items: center;
          margin-top: 4px;
        }
        :global(.like-btn) {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          padding: 6px 14px;
          border: 1.5px solid #d9dce1;
          border-radius: 999px;
          background: transparent;
          font-size: 13px;
          cursor: pointer;
          transition:
            border-color 0.2s,
            color 0.2s;
          color: var(--timeline-text-sub);
          user-select: none;
        }
        :global(.like-btn:hover) {
          border-color: var(--timeline-circle-border);
          color: var(--timeline-circle-border);
        }
        :global(.like-btn.liked) {
          border-color: #e05a5a;
          color: #e05a5a;
        }
        :global(.like-btn .like-icon) {
          font-size: 15px;
          line-height: 1;
          transition: transform 0.15s;
        }
        :global(.like-btn.liked .like-icon) {
          transform: scale(1.25);
        }

        /* YouTube */
        :global(.item-youtube) {
          position: relative;
          width: 100%;
          aspect-ratio: 16 / 9;
          border-radius: 8px;
          overflow: hidden;
          background: #000;
        }
        :global(.item-youtube iframe) {
          position: absolute;
          inset: 0;
          width: 100%;
          height: 100%;
          border: none;
        }

        /* YouTubeサムネイルプレースホルダー */
        :global(.yt-placeholder) {
          position: absolute;
          inset: 0;
          cursor: pointer;
          display: flex;
          align-items: center;
          justify-content: center;
        }
        :global(.yt-placeholder img) {
          position: absolute;
          inset: 0;
          width: 100%;
          height: 100%;
          object-fit: cover;
        }
        :global(.yt-play-btn) {
          position: relative;
          z-index: 1;
          width: 64px;
          height: 44px;
          background: #ff0000;
          border-radius: 12px;
          display: flex;
          align-items: center;
          justify-content: center;
          opacity: 0.92;
          transition:
            opacity 0.2s,
            transform 0.2s;
        }
        :global(.yt-placeholder:hover .yt-play-btn) {
          opacity: 1;
          transform: scale(1.08);
        }
        :global(.yt-play-btn::after) {
          content: "";
          border-style: solid;
          border-width: 9px 0 9px 18px;
          border-color: transparent transparent transparent #fff;
          margin-left: 4px;
        }

        /* レスポンシブ：幅が狭い場合は縦積みに戻す */
        @media (max-width: 900px) {
          :global(.content-col) {
            flex-direction: column;
            align-items: stretch;
          }
          :global(.text-col) {
            flex: 1 1 auto;
          }
        }

        /* ==================== スマホ対応 ==================== */
        @media (max-width: 768px) {
          .contents_texto {
            padding: 10px 12px;
            gap: 12px;
            white-space: normal;
            overflow: hidden;
          }

          .contents_texto h2 {
            flex: 1 1 0%;
            min-width: 0;
            font-size: 15vw;
            overflow-x: hidden;
            overflow-y: hidden;
            white-space: nowrap;
            -webkit-overflow-scrolling: touch;
            scrollbar-width: none;
            font-size: 15vw;
          }

          .content_label {
            top: 10px;
            right: 66px;
            max-width: 88px;
            box-sizing: border-box;
            overflow-x: hidden;
            white-space: nowrap;
            font-size: 11px;
            padding: 4px 8px;
          }

          /* ---- ここから無限ループ・マーキー用のスタイル ---- */
          .marquee-track {
            display: inline-flex;
            white-space: nowrap;
          }

          .marquee-track.is-scrolling {
            animation-name: marquee-loop;
            animation-timing-function: linear;
            animation-iteration-count: infinite;
          }

          @keyframes marquee-loop {
            from {
              transform: translateX(0);
            }
            to {
              transform: translateX(var(--marquee-distance, 0px));
            }
          }

          .marquee-item {
            display: inline-block;
            white-space: nowrap;
          }
          /* ---- ここまで ---- */

          .back_btn {
            width: 40px;
            height: 40px;
          }

          .back_arrow {
            font-size: 16px;
          }

          .chronology {
            max-width: 50vh;
            margin: 0 10px;
          }

          .chronology_title {
            padding: 0.5em 1em;
          }

          .chronology_title h4 {
            font-size: 32px;
            padding: 28px 0 12px 0;
          }

          .chronology_title p {
            font-size: 15px;
            padding-bottom: 28px;
          }

          :global(.timeline-wrap) {
            padding: 32px 16px;
            margin-bottom: 60px;
          }

          :global(.timeline-line) {
            left: 8px;
          }

          :global(.marker-col) {
            width: 16px;
            flex: 0 0 16px;
          }

          :global(.marker-circle) {
            width: 14px;
            height: 14px;
            border-width: 2px;
          }

          :global(.content-col) {
            padding-left: 16px;
            gap: 20px;
          }

          :global(.item-year) {
            font-size: 12px;
          }

          :global(.item-title) {
            font-size: 17px;
            padding-bottom: 8px;
          }

          :global(.item-desc) {
            font-size: 13px;
            line-height: 1.8;
          }

          :global(.item-comment) {
            font-size: 12px;
            padding: 8px 12px;
          }

          :global(.like-btn) {
            font-size: 12px;
            padding: 5px 12px;
          }

          :global(.item-youtube) {
            border-radius: 6px;
          }

          :global(.yt-play-btn) {
            width: 52px;
            height: 36px;
          }
        }
      </style>

      <script>
        const timelineData = [
          {
            year: "2018/09/28",
            title: "初配信",
            desc: "Mirrativにて初配信を行う。",
            comment: "",
            youtube: "",
          },
          {
            year: "2018/09/28",
            title: "自己紹介動画をYouTubeにて投稿",
            desc: "Mirrativ初配信の翌日にYouTubeにて自己紹介動画を投稿。現在はメンバー限定公開となっている。",
            comment:
              "詳細はメン限のため差し控えるが可愛い矢車を確認することができる。",
            youtube: "",
          },
          {
            year: "2018/12/09",
            title: "【矢車りね】スマブラ78時間リレー生配信 その28【黒井しば】",
            desc: "にじさんじ公式チャンネルへの初出演でありYouTube上での初めての配信、そして初コラボでもある初めて尽くしの配信。2026年現在よりも尖っていた（本人談）時代の子供らしい発言や煽りが満載。しばちゃんとの仲の良い駆け引きも見どころ。",
            comment: "comments",
            youtube: "ksgP23ML9x0",
          },
          {
            year: "2018/12/23",
            title: "YouTubeにて初配信",
            desc: "自身のYouTubeチャンネルでの初めての配信。矢車の雑談配信史上公式の最古の記録。最近の出来事や人狼にハマっていることなどを語っている。",
            comment: "comments",
            youtube: "bd3eTSoKR6U",
          },
          {
            year: "2019/06/01",
            title: "矢車りね個人ボイス全部セット発売",
            desc: "先着100名でボイス内容をリクエストすることができた。この時リクエストを出さなかったユーザーにメンヘラトマト収穫ボイスを送付した。",
            comment: "comments",
            youtube: "",
          },
          {
            year: "2019/06/20",
            title: "ライバー用スマホ紛失",
            desc: "ライバー用スマホを失くした雑談配信を行う。「Liver Yo Smartphone」はこの時に生まれた。",
            comment: "comments",
            youtube: "B9SL-dlO9Zo",
          },
          {
            year: "2020/03/31",
            title: "チャンネル登録者数 5万人突破",
            desc: "",
            comment: "",
            youtube: "",
          },
          {
            year: "2020/03/31",
            title: "初めての歌ってみた投稿",
            desc: "矢車初の歌ってみた動画。ヒモと財布の最初で最後の歌ってみたでもある。",
            comment: "縺斐■縺昴≧縺輔∪縺ｧ縺励◆",
            youtube: "3OG0sutK_3U",
          },
          {
            year: "2020/09/06",
            title: "新衣装お披露目",
            desc: "遠北千南卒業後の配信。あちきたの要素を取り入れた飴玉スキンの初お披露目。",
            comment: "",
            youtube: "18KgBxyGl6Y",
          },
          {
            year: "2020/09/06",
            title: "ASMR睡眠導入ボイス販売開始",
            desc: "「矢車りね ASMR睡眠導入ボイス」がにじさんじオフィシャルストアにて販売開始。飛鳥ひなPによるガチ台本となっており、普段の季節ボイスでは聞けない矢車を聞くことが出来る。",
            comment: "",
            youtube: "",
          },
          {
            year: "2022/10/08",
            title: "にじフェス2022不参加",
            desc: "にじフェス2022に不参加だったので、エア感想会を行う。",
            comment: "",
            youtube: "SsUpd6moAII",
          },
          {
            year: "2022/10/08",
            title: "同期歌ってみた投稿",
            desc: "2021年から正式名称が決まった元SEEDs2期生第3弾組【実は同期なんです。】の歌ってみた動画が投稿された。",
            comment: "迷ったときはオトノナルホウへ→",
            youtube: "LTfLs3YH8BY",
          },
          {
            year: "2023/08/01",
            title: "メンヘラトマト収穫ボイス単体販売",
            desc: "メンヘラトマト収穫ボイスがにじさんじオフィシャルストアにて期間限定で単体販売された。メンヘラトマトの矢車とそれを収穫する農家という設定のボイスとなっている。そのタイトルの名に恥じぬ迫真の演技が聞ける素晴らしいボイスとなっている。",
            comment: "現在は販売を終了しています。",
            youtube: "",
          },
          {
            year: "2023/12/06",
            title: "矢車のにじぱぺに自我が芽生える。",
            desc: "矢車のにじぱぺが2023年11月24日より発売開始。所詮ぱぺに自我など芽生えるはずがないと高を括っていた矢車を排し、ぱぺが配信を乗っ取る。なお、この後毎年エイプリルフールの配信枠にはぱぺが現れ、矢車本体に黙って蛮行を働いている。",
            comment: "comments",
            youtube: "hMKc-zWEutI",
          },
          {
            year: "2023/12/16",
            title: "にじらすに上陸",
            desc: "にじ鯖RUSTに参加、やぐるまちたで大暴れの限りを尽くした。「俺らのドリンク汚すなよ」",
            comment: "comments",
            youtube: "5TsfcoMpS-0",
          },
          {
            year: "2023/12/23",
            title: "にじフェス2023 不参加",
            desc: "にじフェス2023にまたしても現地不参加となった矢車。東京ビッグサイト上空から雑談を行い、むせび泣く。",
            comment: "",
            youtube: "Q7u-tdu-4D0",
          },
          {
            year: "2023/12/31",
            title: "登録者数10万人達成",
            desc: "10万人達成するまでの年を越せない耐久配信を行った。極寒の中、冷え切った登録者メーターをチュッチュベロベロしながら無事に10万人を迎え、誰よりも早く年を越した。",
            comment: "",
            youtube: "8T0ckiYjBCs",
          },
          {
            year: "2024/03/10",
            title: "やぐるまちた 謝罪会見",
            desc: "「Untitled Goose Game 〜いたずらガチョウがやって来た!〜」を二人で配信の予定だったが、オンラインで二人プレイができないゲームだったため、やぐるまちたによる謝罪会見が開かれた。",
            comment: "謝罪会見という茶番で約45分間配信をやり切った二人に脱帽。",
            youtube: "mD8roGiO7I8",
          },
          {
            year: "2024/04/01",
            title: "りねぱぺ、トラックに乗りヨーロッパを爆走",
            desc: "エイプリルフールにりねぱぺがチャンネルを乗っ取り、「Euro Truck Simulator 2」の配信を行った。",
            comment: "",
            youtube: "11VM8CRzQ1Q",
          },
          {
            year: "2024/05/10",
            title: "やぐるまちた オフカラオケ",
            desc: "やぐるまちたwith黒井しばによるオフコラボカラオケで、矢車りねがラップを披露。6年目にして卓越したラップスキルが公となり、多くの視聴者に衝撃を与えた。",
            comment: "comments",
            youtube: "-ONerRSopTE",
          },
          {
            year: "2024/05/10",
            title: "ダジャレしか言わないASMR発売",
            desc: "「ダジャレしか言わないASMR 矢車りね」をにじさんじオフィシャルストアにて発売。リスナーに募集したダジャレを読み上げるという非常にニッチな内容となっている。",
            comment: "",
            youtube: "",
          },
          {
            year: "2024/06/20",
            title: "にじGTAに参加、そして伝説へ",
            desc: "にじGTAに参加。黒井しばと共に平和にお散歩を楽しんでいたはずが、衝動的に葛葉を銃撃。後に「矢車事変」「令和のサラエボ事件」と呼ばれるほど本企画のみならず矢車の今後の活動にも大きく影響を及ぼす出来事となった。",
            comment: "",
            youtube: "qYoTp91LxYw",
          },
          {
            year: "2024/09/03",
            title: "手動疑似3.0お披露目",
            desc: "3.0が反抗期を迎え、お披露目予定日に動かすことができなくなったため、お手製の3.0を披露。無事に化け物が誕生し、「山に帰れ」「こっちに来るな」などコメントでは散々な言われようだった。「心は泣いてるのに顔は泣けない…」",
            comment: "comments",
            youtube: "VdheYHhII-0",
          },
          {
            year: "2024/09/08",
            title: "3.0お披露目",
            desc: "数日前に化け物を生み出した矢車だったが、数日後に万を持して3.0のお披露目配信を決行。キラキラ目や涙を流す、青ざめなど色々な表情ができるようになった＋眼鏡も割れるようになった。",
            comment: "どの表情もとんでもなく可愛い。",
            youtube: "3GEGroNyAtI",
          },
          {
            year: "2024/12/31",
            title: "年越し配信で宙へ。",
            desc: "年越しは2025年になる直前でジャンプをし、「地球にいませんでした～w」をする配信を行った。天高く飛んだ矢車はそのまま宇宙空間で新年を迎えた。",
            comment: "",
            youtube: "-NO_1C4pF7o",
          },
          {
            year: "2025/01/25",
            title: "にじさんじスーパーじゃんけん大会 初参加",
            desc: "にじさんじスーパーじゃんけん大会に予選Hブロックで参加。何故かグーしか用意しておらず、敗北",
            comment: "サムネイルのチョキは何だったんだよ",
            youtube: "XIVMojsEjiY",
          },
          {
            year: "2025/02/22",
            title: "にじフェス2025 参加",
            desc: "ついに、にじフェスに現地で参加。Day2ラジオブースにて「樋口楓と矢車りねのなかよしラジオ」を行い、涙の初出演となった。公式企画は約6年ぶりの参加となった。(5:49:50~)",
            comment: "良かったね",
            youtube: "eHEw3TLa8E4",
          },
          {
            year: "2025/04/01",
            title: "りねぱぺ、薬物を栽培・販売",
            desc: "2025年もエイプリルフールにりねぱぺがチャンネルを乗っ取り、「Schedule I」の配信を行った。",
            comment: "comments",
            youtube: "PcVhewAhHBs",
          },
          {
            year: "2025/06/05",
            title: "ファンタジーライフiを配信",
            desc: "「ファンタジーライフi グルグルの竜と時をぬすむ少女」が発売された。兼ねてよりシリーズのファンだった矢車は6月5日から6月12日の間、毎日ファンタジーライフを配信するほどの作品愛を見せた。",
            comment:
              "バグった配信スケジュールに歓喜・戸惑い・恐怖など様々な感情が渦巻いていた",
            youtube: "2dR1fuUQ-qo",
          },
          {
            year: "2025/08/08",
            title: "3Dお披露目配信",
            desc: "3Dお披露目配信を行った。スマートフォン・にじGTA・メンヘラトマトなど、活動の中で起こった出来事を中心に構成されており、これまでの矢車りねの活動を総ざらいしたような内容となっている。",
            comment: "",
            youtube: "7wJJFIFzvvM",
          },
          {
            year: "2025/08/08",
            title: "初のオリジナル楽曲「地獄車」 MV公開",
            desc: "3Dお披露目配信後、「地獄車」のMVが公開された。全ての行いは巡り巡って自分に返ってくるという事を歌った楽曲となっている。楽曲提供はFAKE TYPE.。",
            comment: "",
            youtube: "9xbNg0-MBLY",
          },
          {
            year: "2025/08/17",
            title: "オーストラリア在住が発覚",
            desc: "ヨッシーアイランド配信中あまりにも回線の悪さからプレイを断念。急遽「回線ザコすぎ雑談」へ切り替えることに。リスナーからサーバー位置は東京かと確認された際、オーストラリア在住であることを白状した。",
            comment: "comments",
            youtube: "vWXsdIE9NeE",
          },
          {
            year: "2025/09/28",
            title: "7周年、にじ3Dお披露目、逆凸配信",
            desc: "7周年の記念日は、にじ3Dのお披露目と逆凸配信を行う。普段からお世話になってる先輩や裏でしか交流がなかった後輩との絡み、分厚い同期の絆など非常に見どころの多い内容となっている。",
            comment: "",
            youtube: "_3ygpvBftJk",
          },
          {
            year: "2025/09/28",
            title: "「オトノケ」歌ってみた動画を投稿",
            desc: "7周年記念配信直後にCreepy Nuts「オトノケ」の歌ってみた動画が投稿された。矢車単体では初の歌ってみた動画となる。投稿後、三週間で100万回再生を達成。",
            comment: "今確かに目が合ったな？",
            youtube: "Iy765B4fkmQ",
          },
          {
            year: "2025/11/06",
            title: "初めてのストリートファイター6",
            desc: "ストリートファイター6の配信を行う。コメントや友人、ライバーの助言と座学を経てコツコツと上達をするが、対空にことごとく苦しめられる。",
            comment: "comments",
            youtube: "RMc7sdZkGts",
          },
          {
            year: "2025/12/01",
            title: "オノマトペASMRボイス発売",
            desc: "「オノマトペASMRボイス」をにじさんじオフィシャルストアにて発売。リスナーから募集したオノマトペを読み上げるというダジャレに引き続き、非常にニッチな内容となっている。",
            comment: "",
            youtube: "",
          },
          {
            year: "2025/12/14",
            title:
              "やぐるまちた「マル・マル・モリ・モリ！」歌って踊ってみた動画を投稿",
            desc: "同日に行ったやぐるまちたオフコラボカラオケ配信直後に「マル・マル・モリ・モリ！」の歌って踊ってみた動画を公開。",
            comment: "歌が終わっても最後までみるべき。",
            youtube: "CnkYd2Phwbw",
          },
          {
            year: "2026/02/15",
            title: "ラップ曲いっぱい歌枠配信",
            desc: "3Dお披露目以降、ラップに力を入れていた矢車の集大成のような歌枠。数日前にはメンバー限定で楽曲を覚えるまでひたすら練習をする配信を行うほどには気合が入った歌枠となっている。",
            comment: "comments",
            youtube: "IsazEcayExQ",
          },
          {
            year: "2026/02/15",
            title: "「IRIS OUT」歌ってみた動画を投稿",
            desc: "ラップ曲いっぱい歌枠配信直後に米津玄師「IRIS OUT」歌ってみた動画を公開。自身の長所であるラップ部分を存分に延ばし、生かしながらも、矢車の歌のポテンシャルがキラリと光る歌ってみた動画となっている。",
            comment: "この世で君だけ大正解。",
            youtube: "8Jz9CWNWTKA",
          },
          {
            year: "2026/02/25",
            title: "おしりフリフリ歌って踊ってみた GB",
            desc: "2025年8月8日に行った3Dお披露目配信内で公開されたカバー曲、そのライブ映像が、GB素材になって帰ってきた！",
            comment: "ちょうど切らしてた助かる。",
            youtube: "cfjwpINRJgQ",
          },
          {
            year: "2026/04/01",
            title: "りねぱぺ、泥棒の道へ",
            desc: "2026年もエイプリルフールに体調が悪いという矢車の代わりにりねぱぺがチャンネルを乗っ取り、「Thief Simulator」の配信を行った。",
            comment: "comments",
            youtube: "cb6BuYiKSt4",
          },
        ];

        /* --------------------------------------------------
           いいね数の永続化（localStorage）
           -------------------------------------------------- */
        function likeKey(item: { year: string; title: string }) {
          return `likes_${item.year}_${item.title}`;
        }
        function getLikes(item: { year: string; title: string }) {
          const raw = localStorage.getItem(likeKey(item));
          return raw ? JSON.parse(raw) : { count: 0, liked: false };
        }
        function saveLikes(
          item: { year: string; title: string },
          data: { count: number; liked: boolean },
        ) {
          localStorage.setItem(likeKey(item), JSON.stringify(data));
        }

        /* --------------------------------------------------
           YouTubeサムネイルプレースホルダー
           ビューポートに入るまでiframeを生成しない
           -------------------------------------------------- */
        function buildYoutubePlaceholder(videoId: string): HTMLElement {
          const wrap = document.createElement("div");
          wrap.className = "item-youtube";
          wrap.dataset.ytId = videoId;

          const placeholder = document.createElement("div");
          placeholder.className = "yt-placeholder";
          placeholder.innerHTML = `
            <img src="https://i.ytimg.com/vi/${videoId}/hqdefault.jpg" alt="サムネイル" loading="lazy" />
            <div class="yt-play-btn" aria-hidden="true"></div>
          `;
          placeholder.addEventListener("click", () =>
            swapToIframe(wrap, videoId, true),
          );
          wrap.appendChild(placeholder);
          return wrap;
        }

        function swapToIframe(
          wrap: HTMLElement,
          videoId: string,
          autoplay = false,
        ) {
          const iframe = document.createElement("iframe");
          iframe.src = `https://www.youtube.com/embed/${videoId}${autoplay ? "?autoplay=1" : ""}`;
          iframe.allow =
            "accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture";
          iframe.allowFullscreen = true;
          wrap.innerHTML = "";
          wrap.appendChild(iframe);
        }

        /* --------------------------------------------------
           描画：案A（左テキスト・右メディアの2カラム）
           -------------------------------------------------- */
        function renderTimeline(
          data: {
            year: string;
            title: string;
            desc: string;
            comment?: string;
            youtube?: string;
          }[],
        ) {
          const container = document.getElementById("timeline");
          if (!container) return;
          container.innerHTML = "";

          const lineEl = document.createElement("div");
          lineEl.id = "timelineLine";
          lineEl.className = "timeline-line";
          container.appendChild(lineEl);

          data.forEach((item) => {
            const likeData = getLikes(item);
            const hasMedia = !!item.youtube;

            const itemEl = document.createElement("div");
            itemEl.className = "timeline-item";

            /* 左テキスト列 */
            const textCol = document.createElement("div");
            textCol.className = "text-col";
            textCol.innerHTML = `
              <span class="item-year">${item.year}</span>
              <h3 class="item-title">${item.title}</h3>
              ${item.desc ? `<p class="item-desc">${item.desc}</p>` : ""}
              ${item.comment ? `<p class="item-comment">${item.comment}</p>` : ""}
              <div class="item-footer">
                <button class="like-btn${likeData.liked ? " liked" : ""}" aria-label="いいね">
                  <span class="like-icon">${likeData.liked ? "♥" : "♡"}</span>
                  <span class="like-count">${likeData.count}</span>
                </button>
              </div>
            `;

            /* 右メディア列 */
            const mediaCol = document.createElement("div");
            mediaCol.className = "media-col";
            if (item.youtube) {
              mediaCol.appendChild(buildYoutubePlaceholder(item.youtube));
            }

            /* content-col に両方を入れる */
            const contentCol = document.createElement("div");
            contentCol.className = `content-col${hasMedia ? "" : " no-media"}`;
            contentCol.appendChild(textCol);
            if (hasMedia) contentCol.appendChild(mediaCol);

            itemEl.innerHTML = `
              <div class="marker-col">
                <div class="marker-circle"></div>
              </div>
            `;
            itemEl.appendChild(contentCol);

            /* いいねボタンのイベント */
            const btn = textCol.querySelector(".like-btn") as HTMLButtonElement;
            btn.addEventListener("click", () => {
              const current = getLikes(item);
              const next = current.liked
                ? { count: current.count - 1, liked: false }
                : { count: current.count + 1, liked: true };
              saveLikes(item, next);
              btn.classList.toggle("liked", next.liked);
              btn.querySelector(".like-icon")!.textContent = next.liked
                ? "♥"
                : "♡";
              btn.querySelector(".like-count")!.textContent = String(
                next.count,
              );
            });

            container.appendChild(itemEl);
          });

          updateTimelineLine();
        }

        /* --------------------------------------------------
           縦線の高さ計算
           -------------------------------------------------- */
        function updateTimelineLine() {
          const container = document.getElementById("timeline");
          const line = document.getElementById("timelineLine");
          if (!container || !line) return;
          const circles = container.querySelectorAll(".marker-circle");
          if (circles.length === 0) return;
          const containerTop = container.getBoundingClientRect().top;
          const firstRect = circles[0].getBoundingClientRect();
          const lastRect = circles[circles.length - 1].getBoundingClientRect();
          const top = firstRect.top + firstRect.height / 2 - containerTop;
          const bottom = lastRect.top + lastRect.height / 2 - containerTop;
          (line as HTMLElement).style.top = `${top}px`;
          (line as HTMLElement).style.height = `${bottom - top}px`;
        }

        /* --------------------------------------------------
           IntersectionObserver
           -------------------------------------------------- */
        function observeItems() {
          const itemObserver = new IntersectionObserver(
            (entries) => {
              entries.forEach((entry) => {
                if (entry.isIntersecting) {
                  (entry.target as HTMLElement).classList.add("is-visible");
                  itemObserver.unobserve(entry.target);
                }
              });
            },
            { threshold: 0.08 },
          );

          requestAnimationFrame(() => {
            document.querySelectorAll(".timeline-item").forEach((el, i) => {
              (el as HTMLElement).style.transitionDelay = `${i * 60}ms`;
              itemObserver.observe(el);
            });
          });

          const ytObserver = new IntersectionObserver(
            (entries) => {
              entries.forEach((entry) => {
                if (entry.isIntersecting) {
                  const img = (entry.target as HTMLElement).querySelector(
                    "img",
                  );
                  if (img) img.loading = "eager";
                  ytObserver.unobserve(entry.target);
                }
              });
            },
            { rootMargin: "200px" },
          );
          document
            .querySelectorAll(".item-youtube")
            .forEach((el) => ytObserver.observe(el));
        }

        const BG_REVEAL_MS = 400;
        renderTimeline(timelineData);
        window.addEventListener("resize", updateTimelineLine);
        setTimeout(observeItems, BG_REVEAL_MS);

        document.addEventListener("astro:page-load", () => {
          renderTimeline(timelineData);
          setTimeout(observeItems, BG_REVEAL_MS);
        });

        /* --------------------------------------------------
   タイトル・ラベルの無限ループ・マーキー表示
   はみ出している要素だけ、テキストを複製して隙間なく
   並べ、CSSアニメーションで左に流し続ける。
   1周分スライドし終わったら（＝複製2つ目の先頭が
   元の位置に来たら）継ぎ目なく最初から繰り返される。
   -------------------------------------------------- */
        const MARQUEE_SPEED = 60; // 数値を大きくするほど速く流れる（px/秒）
        const MARQUEE_GAP = 48; // テキストの最後と、次の頭との間隔(px)
        const MARQUEE_START_DELAY = 1000; // 表示されてから動き出すまでの待ち時間(ms)

        function initAutoScrollTitles() {
          const targets = document.querySelectorAll<HTMLElement>(
            ".contents_texto h2, .content_label",
          );

          targets.forEach((el) => {
            if (el.dataset.marqueeBound === "true") return;

            const overflowAmount = el.scrollWidth - el.clientWidth;
            if (overflowAmount <= 1) return; // はみ出していなければ何もしない

            el.dataset.marqueeBound = "true";

            const text = el.textContent ?? "";
            el.textContent = "";

            const track = document.createElement("span");
            track.className = "marquee-track";

            const item1 = document.createElement("span");
            item1.className = "marquee-item";
            item1.textContent = text;

            const item2 = document.createElement("span");
            item2.className = "marquee-item";
            item2.textContent = text;
            item2.setAttribute("aria-hidden", "true");
            item2.style.paddingLeft = `${MARQUEE_GAP}px`;

            track.appendChild(item1);
            track.appendChild(item2);
            el.appendChild(track);

            setTimeout(() => {
              // 実測した1コピー分の幅＋隙間 だけ流せば、複製2つ目が
              // ちょうど元の位置に来て継ぎ目なくループする
              const distance =
                item1.getBoundingClientRect().width + MARQUEE_GAP;
              const duration = distance / MARQUEE_SPEED;
              track.style.setProperty("--marquee-distance", `-${distance}px`);
              track.style.animationDuration = `${duration}s`;
              track.classList.add("is-scrolling");
            }, MARQUEE_START_DELAY);
          });
        }

        initAutoScrollTitles();
        document.addEventListener("astro:page-load", initAutoScrollTitles);

        /* Adobe Fonts等、フォントの非同期読み込みで文字幅が変わったあとに
   もう一度オーバーフロー判定をやり直す(フォールバックフォント表示中に
   「はみ出していない」と誤判定されるのを防ぐ) */
        if (
          document.fonts &&
          typeof document.fonts.ready?.then === "function"
        ) {
          document.fonts.ready.then(() => {
            requestAnimationFrame(initAutoScrollTitles);
          });
        }
        // 上記が使えない/間に合わない場合の保険として少し遅れて再チェック
        setTimeout(initAutoScrollTitles, 1500);
      </script>
    </div>
  </body>
</BaseLayout>
